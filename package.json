{
  "name": "flowtask-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for FlowTask application",
  "main": "backend/server.js",
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "deploy": "chmod +x ./frontend/node_modules/.bin/vite && npm run build && npm run start"
  },
  "keywords": ["flowtask", "monorepo", "react", "express", "mongodb"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
